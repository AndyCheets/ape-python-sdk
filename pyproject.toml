[project]
name = "ape-sdk"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pika>=1.3", "SQLAlchemy>=2.0", "pymysql>=1.1", "cryptography", "pydantic-settings>=2.2", "pydantic>=2.6"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
